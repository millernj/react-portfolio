import React from 'react';
import Container from '../components/Container';

const Portfolio = () => {
  return (
    <Container>
        Anything Else
    </Container>
  );
};

export default Portfolio;
